// Determinacy
LIB "polyclass.lib";
LIB "arnoldgit.lib";
ring R=0,(x,y),ds;
poly g = (y3 - x2)*(y2-x3)+y26;
determinacyBound(g);  // 35
determinacyBound(g);  // 35
list l = determinacyBound(g);  // 35
l;
basering;
// Determinacy
LIB "polyclass.lib";
LIB "arnoldgit.lib";
ring R=0,(x,y),ds;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
gOrig = substitute(gOrig, x, x+y);
gOrig = monic(gOrig);  
determinacyBound(gOrig);  // 35
1+1;
gOrig;
gOrig;
;
// Determinacy
LIB "polyclass.lib";
LIB "arnoldgit.lib";
ring R=0,(x,y),ds;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
gOrig = substitute(gOrig, x, x+y);
gOrig = monic(gOrig);  
determinacyBound(gOrig);  // 35
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y17 - x2) + x25;
integralBasis(g,2);
poly g1 = (y17 - x2);
integralBasis(g,2);
poly g1 = (y17 - x2);
integralBasis(g,1);
poly g1 = -(y17 - x2);
integralBasis(g,1);
poly g1 = -(y17 - x2);
integralBasis(g1,1);
poly g1 = x2-y17;
integralBasis(g1,1);
LIB"integralbasis.lib";
poly g1 = x2-y17;
integralBasis(g1,1);
LIB"integralbasis.lib";
poly g1 = x2-y17;
LIB"integralbasis.lib";
poly g1 = x2-y17;
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g1 = x2-y17;
integralBasis(g1,1);
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g1 = x2-y17;
integralBasis(g1,1);
basering;
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y17 - x2) + x25;
integralBasis(g,2);
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g1 = y2-x17 + x30;
integralBasis(g1,2);
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g1 = y2-x17;
integralBasis(g1,2);
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g1 = x2-y17;
integralBasis(g1,1);
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g1 = x2-y17;
integralBasis(g1,1);
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y17 - x2) + x25;
integralBasis(g,2);
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g1 = x2-y17;
integralBasis(g1,1);
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g1 = y2-x17;
integralBasis(g1,2);
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y17 - x2) + x25;
integralBasis(g,2);
poly g1 = -(y17 - x2);
integralBasis(g,1);
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y17 - x2) + x25;
integralBasis(g,2);
poly g1 = -(y17 - x2);
integralBasis(g,1);
g;
poly g1 = (y17 - x2);
integralBasis(g,2);
g;
poly g1 = -(y17 - x2);
integralBasis(g,1);
g;
determinacyBound(gOrig);  // 35
LIB "polyclass2.lib";
LIB "arnoldclassify.lib";
determinacyBound(gOrig);  // 35
LIB "deltaInvariant.lib";
determinacyBound(gOrig);  // 35
LIB "deltaInvariant.lib";
determinacyBound(g);  // 35
LIB "deltaInvariant.lib";
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y17 - x2) + x25;
determinacyBound(g);  // 35
LIB"integralbasis.lib";
LIB"deltaInvariant.lib";
ring R = 0, (x,y), dp;
poly g = (y17 - x2) + x25;
determinacyBound(g);  // 35
LIB "deltaInvariant.lib";
LIB "integralbasis.lib";
LIB "polyclass2.lib";
LIB "arnoldclassify.lib";
ring R=0,(x,y),ds;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
gOrig = substitute(gOrig, x, x+y);
gOrig = monic(gOrig);  
determinacyBound(gOrig);  // 35
poly g = (y17 - x2) + y20 + x25;
determinacyBound(g);  // 35
LIB "polyclass.lib";
LIB "arnoldgit.lib";
ring R=0,(x,y),ds;
poly g = (y3 - x2)*(y2-x3)+y26;
determinacyBound(g);  // 35
LIB"integralbasis.lib";
LIB "polyclass.lib";
LIB "arnoldgit.lib";
ring R = 0, (x,y), ds;
poly g = (y17 - x2) + x25;
determinacyBound(g);  // 35
poly g = (y17 - x2) + x35;
determinacyBound(g);  // 35
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y37 - x2);
milnor(g);
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y3 - x2);
milnor(g);
ring S = 2, (x,y), dp;
poly g = (y3 - x2);
milnor(g);
ring S = 5, (x,y), dp;
poly g = (y3 - x2);
milnor(g);
ring S = 3, (x,y), dp;
poly g = (y3 - x2);
milnor(g);
LIB"integralbasis.lib";
ring S = 3, (x,y), dp;
poly g = (y3 - x2);
milnor(g);
LIB"integralbasis.lib";
ring S = 0, (x,y), dp;
poly g = (y17 - x15);
milnor(g);
LIB"integralbasis.lib";
ring S = 0, (x,y), dp;
poly g = (y5 - x3)^4 + 3y7x3;
milnor(g);
LIB"integralbasis.lib";
ring S = 0, (x,y), dp;
poly g = (y15 - x12)^4 + 3y68x40;
milnor(g);
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
gOrig = substitute(gOrig, x, x+y);
gOrig = monic(gOrig);  
gOrig;
milnor(gOrig);
LIB"integralbasis.lib";
ring R = 2, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
gOrig = substitute(gOrig, x, x+y);
gOrig = monic(gOrig);  
milnor(gOrig);
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
gOrig = substitute(gOrig, x, x+y);
gOrig = monic(gOrig);  
milnor(gOrig);
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y7 - x2)*(y7-x3)+y15 + x15;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
gOrig = substitute(gOrig, x, x+y);
gOrig = monic(gOrig);  
milnor(gOrig);
LIB"integralbasis.lib";
ring R = 2, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
gOrig = substitute(gOrig, x, x+y);
gOrig = monic(gOrig);  
milnor(gOrig);
g;
gOrig;
LIB "polyclass.lib";
LIB "arnoldgit.lib";
ring R=0,(x,y),ds;
poly g = (y7 - x2)*(y7-x3)+y15 + x15;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
gOrig = substitute(gOrig, x, x+y);
gOrig = monic(gOrig);  
determinacyBound(gOrig);  // 35
LIB"integralbasis.lib";
ring R = 11, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
gOrig = substitute(gOrig, x, x+y);
gOrig = monic(gOrig);  
milnor(gOrig);
LIB"integralbasis.lib";
ring R = 3, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
gOrig = substitute(gOrig, x, x+y);
gOrig = monic(gOrig);  
milnor(gOrig);
LIB"integralbasis.lib";
ring R = 5, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
gOrig = substitute(gOrig, x, x+y);
gOrig = monic(gOrig);  
milnor(gOrig);
LIB"integralbasis.lib";
ring R = 7, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
gOrig = substitute(gOrig, x, x+y);
gOrig = monic(gOrig);  
milnor(gOrig);
milnorNumber(gOrig);
tjurina(gOrig);
gOrig;
basering;
LIB"integralbasis.lib";
ring S = 0, (x,y), dp;
poly g = (y15 - x12)^4 + 3y68x40;
milnor(g);
LIB "polyclass.lib";
LIB "arnoldgit.lib";
LIB"integralbasis.lib";
ring S = 0, (x,y), dp;
poly g = (y15 - x12)^4 + 3y68x40;
milnor(g);
milnorNumber(g);
Poly G = g;
milnorNumber(G);
exit
;
LIB "polyclass.lib";
LIB "arnoldgit.lib";
ring R=0,(x,y),ds;
poly g = (y3 - x2)*(y2-x3)+y26;
determinacyBound(g);  // 35
LIB "polyclass.lib";
LIB "arnoldgit.lib";
ring R=0,(x,y),ds;
poly g = (y3 - x2)*(y2-x3)+y26;
determinacyBound(g);  // 5
map phi = R,x+y^2+x^2+x*y+x^2*y+x*y^3-x^6*y^100, y+y^2+2*x^2+x*y+y*x^2+y^2*x+x*y^4+x^2*y^123;
poly gOrig = phi(g);
gOrig = substitute(gOrig, x, x+y);
//gOrig = monic(gOrig);  
determinacyBound(gOrig);  // 35
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
gOrig = substitute(gOrig, x, x+y);
gOrig = monic(gOrig);  
determinacyBound(gOrig);  // 5
LIB "polyclass.lib";
LIB "arnoldgit.lib";
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
gOrig = substitute(gOrig, x, x+y);
gOrig = monic(gOrig);  
determinacyBound(gOrig);  // 5
LIB "polyclass.lib";
LIB "arnoldgit.lib";
LIB"integralbasis.lib";
ring R = 0, (x,y), ds;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
gOrig = substitute(gOrig, x, x+y);
gOrig = monic(gOrig);  
determinacyBound(gOrig);  // 5
puiseuxList(gOrig, -1, 1, 2);  // Same Puiseux expansions?
// We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 35;
int yDegTrunc = 35;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
puiseuxList(gTrunc, -1, 1, 2);  // Same Puiseux expansions
gOrig;
gTrunc;
// We truncate all monomials of deg > 30 and compare the Puiseux expansions
int xDegTrunc = 30;
int yDegTrunc = 30;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
puiseuxList(gTrunc, -1, 1, 2);  // Same Puiseux expansions
/ We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 35;
int yDegTrunc = 35;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
puiseuxList(gTrunc, -1, 1, 2);  // Same Puiseux expansions
// We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 35;
int yDegTrunc = 35;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
puiseuxList(gTrunc, -1, 1, 2);  // Same Puiseux expansions
// We truncate all monomials of deg > 30 and compare the Puiseux expansions
int xDegTrunc = 30;
int yDegTrunc = 30;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
puiseuxList(gTrunc, -1, 1, 2);  // Same Puiseux expansions
// We truncate all monomials of deg > 20 and compare the Puiseux expansions
int xDegTrunc = 20;
int yDegTrunc = 20;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
puiseuxList(gTrunc, -1, 1, 2);  // Same Puiseux expansions
// We truncate all monomials of deg > 20 and compare the Puiseux expansions
int xDegTrunc = 25;
int yDegTrunc = 25;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
puiseuxList(gTrunc, -1, 1, 2);  // Same Puiseux expansions
// We truncate all monomials of deg > 20 and compare the Puiseux expansions
int xDegTrunc = 24;
int yDegTrunc = 24;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
puiseuxList(gTrunc, -1, 1, 2);  // Same Puiseux expansions
// We truncate all monomials of deg > 20 and compare the Puiseux expansions
int xDegTrunc = 23;
int yDegTrunc = 23;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
puiseuxList(gTrunc, -1, 1, 2);  // Same Puiseux expansions
LIB "polyclass.lib";
LIB "arnoldgit.lib";
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
gOrig = substitute(gOrig, x, x+y);
gOrig = monic(gOrig);  
// We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 35;
int yDegTrunc = 35;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
integralBasis(gTrunc, 2);  // Same Puiseux expansions
LIB "polyclass.lib";
LIB "arnoldgit.lib";
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
// We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 35;
int yDegTrunc = 35;
poly gTrunc = gOrig;
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
integralBasis(gTrunc, 2);  // Same Puiseux expansions
// We now check at which degree can we truncate the polynomial f to
// get the same integral basis
LIB "polyclass.lib";
LIB "arnoldgit.lib";
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
// We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 35;
int yDegTrunc = 35;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
integralBasis(gTrunc, 2);  // Same Puiseux expansions
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
// We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 35;
int yDegTrunc = 35;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
printlevel = 5;
integralBasis(gTrunc, 2);  // Same Puiseux expansions
LIB "polyclass.lib";
LIB "arnoldgit.lib";
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
// We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 35;
int yDegTrunc = 35;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
printlevel = 5;
integralBasis(gTrunc, 2, "atOrigin");  // Same Puiseux expansions
// We truncate all monomials of deg >= 24 and compare the Puiseux expansions
int xDegTrunc = 24;
int yDegTrunc = 24;
poly gTrunc = gOrig;
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
printlevel = 5;
integralBasis(gTrunc, 2, "atOrigin");  // Same Puiseux expansions
// We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 35;
int yDegTrunc = 35;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
printlevel = 5;
list l35 = integralBasis(gTrunc, 2, "atOrigin");  // Same Puiseux expansions
// We truncate all monomials of deg >= 24 and compare the Puiseux expansions
int xDegTrunc = 24;
int yDegTrunc = 24;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
printlevel = 5;
list l24 = integralBasis(gTrunc, 2, "atOrigin");  // Same Puiseux expansions
l24[1];
reduce(l24[1], l35[1]);
reduce(l24[1], groebner(l35[1]));
l24
;
l35;
// We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 35;
int yDegTrunc = 35;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
printlevel = 5;
list l35 = integralBasis(gTrunc, 2, "atOrigin", "locBasis");  // Same Puiseux expansions
// We truncate all monomials of deg >= 24 and compare the Puiseux expansions
int xDegTrunc = 24;
int yDegTrunc = 24;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
printlevel = 5;
list l24 = integralBasis(gTrunc, 2, "atOrigin", "locBasis");  // Same Puiseux expansions
reduce(l35[1], groebner(l24[1]));
// We now check at which degree can we truncate the polynomial f to
// get the same integral basis
LIB "polyclass.lib";
LIB "arnoldgit.lib";
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
// We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 35;
int yDegTrunc = 35;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
printlevel = 5;
list l35 = integralBasis(gTrunc, 2, "atOrigin", "locBasis");  // Same Puiseux expansions
// We truncate all monomials of deg >= 24 and compare the Puiseux expansions
int xDegTrunc = 24;
int yDegTrunc = 24;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
printlevel = 5;
list l24 = integralBasis(gTrunc, 2, "atOrigin", "locBasis");  // Same Puiseux expansions
reduce(l35[1], l24[1]);
l35;
l24;
// We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 35;
int yDegTrunc = 35;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
printlevel = 5;
list l35 = integralBasis(gTrunc, 2, "atOrigin", "locBasis");  // Same Puiseux expansions
// We truncate all monomials of deg >= 24 and compare the Puiseux expansions
int xDegTrunc = 44;
int yDegTrunc = 44;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
printlevel = 5;
list l24 = integralBasis(gTrunc, 2, "atOrigin", "locBasis");  // Same Puiseux expansions
reduce(l35[1], l24[1]);
// We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 55;
int yDegTrunc = 55;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
printlevel = 5;
list l35 = integralBasis(gTrunc, 2, "atOrigin", "locBasis");  // Same Puiseux expansions
// We truncate all monomials of deg >= 24 and compare the Puiseux expansions
int xDegTrunc = 44;
int yDegTrunc = 44;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
printlevel = 5;
list l24 = integralBasis(gTrunc, 2, "atOrigin", "locBasis");  // Same Puiseux expansions
reduce(l35[1], l24[1]);
int xDegTrunc = 55;
int yDegTrunc = 55;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
//gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
printlevel = 5;
list l35 = integralBasis(gTrunc, 2, "atOrigin", "locBasis");  // Same Puiseux expansions
// We truncate all monomials of deg >= 24 and compare the Puiseux expansions
int xDegTrunc = 44;
int yDegTrunc = 44;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
//gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
printlevel = 5;
list l24 = integralBasis(gTrunc, 2, "atOrigin", "locBasis");  // Same Puiseux expansions
reduce(l35[1], l24[1]);
LIB "polyclass.lib";
LIB "arnoldgit.lib";
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
// We truncate all monomials of deg >= 24 and compare the Puiseux expansions
int xDegTrunc = 44;
int yDegTrunc = 44;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
//gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
list l24 = integralBasis(gTrunc, 2, "atOrigin", "locBasis");  // Same Puiseux expansions
gTrunc;
puiseuxList(gTrunc, -1, 1, 2);  // Not the same Puiseux expansions!
coef(gTrunc);
coefs(gTrunc);
coeff(gTrunc);
coeff(gTrunc, "xy");
coeffs(gTrunc, y);
gTrunc;
LIB "polyclass.lib";
LIB "arnoldgit.lib";
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
// We truncate all monomials of deg >= 24 and compare the Puiseux expansions
int xDegTrunc = 44;
int yDegTrunc = 44;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
puiseuxList(gTrunc, -1, 1, 2);  // Not the same Puiseux expansions!
list l24 = integralBasis(gTrunc, 2, "atOrigin", "locBasis");  // Same Puiseux expansions
l;
l24;
LIB "polyclass.lib";
LIB "arnoldgit.lib";
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
// We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 55;
int yDegTrunc = 55;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
//gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
list l35 = integralBasis(gTrunc, 2, "atOrigin", "locBasis", "noOpti");  // Same Puiseux expansions
l35;
// We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 55;
int yDegTrunc = 55;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
list l35 = integralBasis(gTrunc, 2, "atOrigin", "locBasis", "noOpti");  // Same Puiseux expansions
l35;
printlevel = 5;
// We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 55;
int yDegTrunc = 55;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
list l35 = integralBasis(gTrunc, 2, "atOrigin", "locBasis", "noOpti");  // Same Puiseux expansions
LIB "polyclass.lib";
LIB "arnoldgit.lib";
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
printlevel = 5;
// We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 55;
int yDegTrunc = 55;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
list l35 = integralBasis(gTrunc, 2, "atOrigin", "locBasis", "noOpti");  // Same Puiseux expansions
LIB "polyclass.lib";
LIB "arnoldgit.lib";
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
printlevel = 5;
// We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 55;
int yDegTrunc = 55;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
list l35 = integralBasis(gTrunc, 2, "atOrigin", "locBasis", "noOpti");  // Same Puiseux expansions
l35;
coeffs(f, y);
coeffs(gTrunc, y);
subst(gTrunc,x,0);
subst(gOrig,x,0);
subst(g,x,0);
LIB "polyclass.lib";
LIB "arnoldgit.lib";
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
printlevel = 5;
// We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 55;
int yDegTrunc = 55;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
list l35 = integralBasis(gTrunc, 2, "atOrigin", "locBasis", "noOpti");  // Same Puiseux expansions
l35;
// We truncate all monomials of deg >= 24 and compare the Puiseux expansions
int xDegTrunc = 44;
int yDegTrunc = 44;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
list l24 = integralBasis(gTrunc, 2, "atOrigin", "locBasis", "noOpti");  // Same Puiseux expansions
l24;
l35;
// We truncate all monomials of deg >= 24 and compare the Puiseux expansions
int xDegTrunc = 24;
int yDegTrunc = 24;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
list l24 = integralBasis(gTrunc, 2, "atOrigin", "locBasis", "noOpti");  // Same Puiseux expansions
l24;
l35;
reduce(l35[1], l24[1]);
reduce(l35[1], groebner(l24[1]));
// We truncate all monomials of deg >= 24 and compare the Puiseux expansions
int xDegTrunc = 20;
int yDegTrunc = 20;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
list l20 = integralBasis(gTrunc, 2, "atOrigin", "locBasis", "noOpti");  // Same local integral basis
l20;
// We truncate all monomials of deg >= 24 and compare the Puiseux expansions
int xDegTrunc = 23;
int yDegTrunc = 23;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
list l20 = integralBasis(gTrunc, 2, "atOrigin", "locBasis", "noOpti");  // Not the same local integral basis!
l20;
l35;
// We truncate all monomials of deg >= 24 and compare the Puiseux expansions
int xDegTrunc = 22;
int yDegTrunc = 22;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
list l22 = integralBasis(gTrunc, 2, "atOrigin", "locBasis", "noOpti");  // Not the same local integral basis!
LIB "polyclass.lib";
LIB "arnoldgit.lib";
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
// We truncate all monomials of deg >= 24 and compare the Puiseux expansions
int xDegTrunc = 21;
int yDegTrunc = 21;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
list l22 = integralBasis(gTrunc, 2, "atOrigin", "locBasis", "noOpti");  // Not the same local integral basis!
l22;
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
printlevel = 5;
// We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 35;
int yDegTrunc = 35;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
list l35 = integralBasis(gTrunc, 2, "atOrigin", "locBasis", "noOpti");  // Same local integral basis
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
printlevel = 5;
// We truncate all monomials of deg > 35 and compare the Puiseux expansions
int xDegTrunc = 35;
int yDegTrunc = 35;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
list l35 = integralBasis(gTrunc, 2, "atOrigin", "locBasis", "noOpti");  // Same local integral basis
l35;
list l35o = integralBasis(gTrunc, 2, "atOrigin", "locBasis");  // Same local integral basis
l35o;
reduce(l35o[1], groebenr(l35[1]);
reduce(l35o[1], groebner(l35[1]);
reduce(l35o[1], grobner(l35[1]);
reduce(l35o[1], groebner(l35[1]));
reduce(l35[1], groebner(l35o[1]));
reduce(l35o[1][5], x18);
LIB"integralbasis.lib";
ring S = 0, (x,y), ds;
poly g = (y15 - x12)^4 + 3y68x40;
determinacyBound(g);  // 35
LIB "polyclass.lib";
LIB "arnoldgit.lib";
ring S = 0, (x,y), ds;
poly g = (y15 - x12)^4 + 3y68x40;
determinacyBound(g);  // 35
LIB "polyclass.lib";
LIB "arnoldgit.lib";
ring S = 0, (x,y), ds;
poly g = (y15 - x12)^4 + 3y68x40;
determinacyBound(g);  // 35
ring R = 0, (x,y), dp;
poly g = (y15 - x12)^4 + 3y68x40;
list l35o = integralBasis(g, 2, "atOrigin", "locBasis");  // Same local integral basis
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y15 - x12)^4 + 3y68x40;
list l35o = integralBasis(g, 2, "atOrigin", "locBasis");  // Same local integral basis
LIB "polyclass.lib";
LIB "arnoldgit.lib";
ring S = 0, (x,y), ds;
poly g = (y15 - x12)^4 + 3y68x40 + y70;
determinacyBound(g);  // 173
poly g = (y15 - x12)^4 + 3y68x40 + y120;
determinacyBound(g);  // 173
poly g = (y15 - x12)^4 + 3y68x40 + y150;
determinacyBound(g);  // 173
poly g = (y15 - x12)^4 + 3y68x40 + y120;
determinacyBound(g);  // 173
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y15 - x12)^4 + 3y68x40 + y120;
list l35o = integralBasis(g, 2, "atOrigin", "locBasis");  // Same local integral basis
LIB "polyclass.lib";
LIB "arnoldgit.lib";
ring S = 0, (x,y), ds;
poly g = (y15 - x12)^2 + 3y68x40 + y120;
determinacyBound(g);  // 173
printlevel = 5;
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y15 - x12)^2 + 3y68x40 + y120;
list l35o = integralBasis(g, 2, "atOrigin", "locBasis");  // Same local integral basis
f;
g;
puiseuxList(g, -1, 1, 2);  // Same Puiseux expansions
printlevel = 5;
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y15 - x12)^2 + 3y68x40 + y120;
list l35o = integralBasis(g, 2, "atOrigin", "locBasis");  // Same local integral basis
printlevel = 5;
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y15 - x12)^2 + 3y68x40 + y120;
list l35o = integralBasis(g, 2, "atOrigin", "locBasis");  // Same local integral basis
printlevel = 5;
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y15 - x12)^2 + 3y68x40 + y120;
list l35o = integralBasis(g, 2, "atOrigin", "locBasis");  // Same local integral basis
printlevel = 25;
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y15 - x12)^2 + 3y68x40 + y120;
list l35o = integralBasis(g, 2, "atOrigin", "locBasis");  // Same local integral basis
printlevel = 25;
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y15 - x12)^2 + 3y68x40 + y120;
list l35o = integralBasis(g, 2, "atOrigin", "locBasis");  // Same local integral basis
printlevel = 25;
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y15 - x12)^2 + 3y68x40 + y120;
list l35o = integralBasis(g, 2, "atOrigin", "locBasis");  // Same local integral basis
ib
ibSeg
pSegment
ibSeg
printlevel = 25;
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y15 - x12)^2 + 3y68x40 + y120;
list l35o = integralBasis(g, 2, "atOrigin", "locBasis");  // Same local integral basis
printlevel = 25;
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y15 - x12)^2 + 3y68x40 + y120;
list l35o = integralBasis(g, 2, "atOrigin", "locBasis");  // Same local integral basis
polySet
classes
ordExpAtPoly
polySet[2]
classes[1][1]
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y15 - x12)^2 + 3y68x40 + y120;
puiseuxList(g, -1, 1, 2);  // Same Puiseux expansions
LIB"integralbasis.lib";
ring R = 0, (x,y), dp;
poly g = (y15 - x12)^2 + 3y68x40 + y120;
list l35o = integralBasis(g, 2, "atOrigin", "locBasis", "noOpti"); // Same local integral basis
l35o;
LIB "polyclass.lib";
LIB "arnoldgit.lib";
ring R=0,(x,y),ds;
poly g = (y5 - x7)*(y7-x4)*(y4-x11)+y20;
determinacyBound(g);  // 5
list l1 = integralBasis(g, 2, "atOrigin", "locBasis");  // Same local integral basis
LIB"integralbasis.lib";
list l1 = integralBasis(g, 2, "atOrigin", "locBasis");  // Same local integral basis
l1;
g;
deg(g);
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
determinacyBound(gOrig);  // 33
deg gOrig;
deg(gOrig);
list l1 = integralBasis(gOrig, 2, "atOrigin", "locBasis");  // Same local integral basis
l1;
int xDegTrunc = 35;
int yDegTrunc = 35;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc;
coeffs(gTrunc);
int xDegTrunc = 35;
int yDegTrunc = 35;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
list l1 = integralBasis(gTrunc, 2, "atOrigin", "locBasis");  // Same local integral basis
l1;
gTrunc0 = substitute(gOrig, x, x+y);
list l1 = integralBasis(gTrunc0, 2, "atOrigin", "locBasis"); 
poly gTrunc0 = substitute(gOrig, x, x+y);
LIB "polyclass.lib";
LIB "arnoldgit.lib";
ring R=0,(x,y),ds;
poly g = (y5 - x7)*(y7-x4)*(y4-x11)+y20;
determinacyBound(g);  // 33
LIB"integralbasis.lib";
list l1 = integralBasis(g, 2, "atOrigin", "locBasis");  // Same local integral basis
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
determinacyBound(gOrig);  // 33
poly gTrunc0 = substitute(gOrig, x, x+y);
list l1 = integralBasis(gTrunc0, 2, "atOrigin", "locBasis"); 
l1;
list l0 = l1;
poly gNoTrunc = substitute(gOrig, x, x+y);
list l0 = integralBasis(gNoTrunc, 2, "atOrigin", "locBasis"); 
//Truncation
nt xDegTrunc = 35;
int yDegTrunc = 35;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
list l1 = integralBasis(gTrunc, 2, "atOrigin", "locBasis");  // Same local integral basis
l1;
l0;
l0;
l1;
reduce(l0, l1);
reduce(l0[0], l1[0]);
l0[1]
;
reduce(l0[1], l1[1]);
reduce(l0[1], groebner(l1[1]));
reduce(l1[1], groebner(l0[1]));
int xDegTrunc = 19;
int yDegTrunc = 19;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
list l1 = integralBasis(gTrunc, 2, "atOrigin", "locBasis");  // Same local integral basis
[200~LIB "polyclass.lib";
LIB "arnoldgit.lib";
ring R=0,(x,y),ds;
poly g = (y5 - x7)*(y7-x4)*(y4-x11)+y20;
determinacyBound(g);  // 33
LIB"integralbasis.lib";
list l1 = integralBasis(g, 2, "atOrigin", "locBasis");  // Same local integral basis
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
determinacyBound(gOrig);  // 33
poly gNoTrunc = substitute(gOrig, x, x+y);
list l0 = integralBasis(gNoTrunc, 2, "atOrigin", "locBasis"); 
int xDegTrunc = 22;
int yDegTrunc = 22;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
list l1 = integralBasis(gTrunc, 2, "atOrigin", "locBasis");  // Same local integral basis
;
;
LIB "polyclass.lib";
LIB "arnoldgit.lib";
ring R=0,(x,y),ds;
poly g = (y5 - x7)*(y7-x4)*(y4-x11)+y20;
determinacyBound(g);  // 33
LIB"integralbasis.lib";
map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
determinacyBound(gOrig);  // 33
poly gNoTrunc = substitute(gOrig, x, x+y);
list l0 = integralBasis(gNoTrunc, 2, "atOrigin", "locBasis"); 
int xDegTrunc = 25;
int yDegTrunc = 25;
poly gTrunc = gOrig;
gTrunc = reduce(gTrunc, x^xDegTrunc);
gTrunc = reduce(gTrunc, y^yDegTrunc);
gTrunc = substitute(gTrunc, x, x+y);
gTrunc = monic(gTrunc);  
list l1 = integralBasis(gTrunc, 2, "atOrigin", "locBasis");  // Same local integral basis
l0;
l1;
reduce(l0, groebner(l1));
;
reduce(l0[1], groebner(l1[1]));
reduce(l1[1], groebner(l0[1]));
l1;
l0;
exit
;
LIB"integralbasis.lib";
ring R = 0, (x,y), ds;
poly g = (y11 - x2)*(y13-x3)+y25 + x25;
puiseuxList(g, -1, 1, 2);  // Original Puiseux expansions?

map phi = R,x+y^2-y^6, y+y^2+y^6;
poly gOrig = phi(g);
gOrig = substitute(gOrig, x, x+y);
gOrig = monic(gOrig);  

puiseuxList(gOrig, -1, 1, 2);  // Original Puiseux expansions?

gOrig = substitute(g, x, x+y);
gOrig = monic(gOrig);  
puiseuxList(gOrig, -1, 1, 2);  // Original Puiseux expansions?

gOrig = substitute(g, y, x+y);
gOrig = monic(gOrig);  
puiseuxList(gOrig, -1, 1, 2);  // Original Puiseux expansions?

gOrig = substitute(g, y, x^2 + 7x +y);
gOrig = monic(gOrig);  
puiseuxList(gOrig, -1, 1, 2);  // Original Puiseux expansions?

gOrig;
g;
gOrig = substitute(g, y, 3x*y + x^2 - 7x +y);
gOrig = monic(gOrig);  
puiseuxList(gOrig, -1, 1, 2);  // Original Puiseux expansions?

gOrig = substitute(g, y, 3x*y^2 + x^2 - 7x +y);
gOrig = monic(gOrig);  
puiseuxList(gOrig, -1, 1, 2);  // Original Puiseux expansions?

gOrig = substitute(g, y, 3x*y^2 +y);
gOrig = monic(gOrig);  
puiseuxList(gOrig, -1, 1, 2);  // Original Puiseux expansions?

gOrig;
gOrig = substitute(g, y, 3x*y^2 +y);
//gOrig = monic(gOrig);  
puiseuxList(gOrig, -1, 1, 2);  // Original Puiseux expansions?

gOrig = substitute(g, y, 7x + 2x^2 - 3x*y^2 +y);
//gOrig = monic(gOrig);  
puiseuxList(gOrig, -1, 1, 2);  // Original Puiseux expansions?

gOrig;
